.DS_Store

# skills CLI: multi-agent symlink wrappers (we houden alleen .agents/skills)
.adal/
.augment/
.bob/
.claude/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
.agents/
.cursor/

# Third-party install PDFs at the repo root or one directory deep (not for
# git — large binaries, no diff). PDFs that ARE part of the documentation
# (under resources_and_docs/) are still committed.
/*.pdf
/*/*.pdf
!resources_and_docs/**/*.pdf
# Per-developer PDF assets that landed in the repo at some point but
# should not be committed (large binaries, no diff, vendor-licensed).
# Add new entries below as needed.
Installatie*.pdf
*Installatie*.pdf

.venv/
__pycache__/
*.pyc
probe_output.log
dimmer_re_manifest.log
devices.discovered.json
devices.json.pre-scratch
devices.json.bak
# Runtime instance_id for HA discovery (HA-discovery advertiser persists
# this on first start so the gateway has a stable UUID across restarts;
# it is a per-install artifact, not source).
instance_id
captures/
tmp/
*.pcapng
*.pcap
!resources_and_docs/pcap_archive/long_capture.pcapng
!tests/fixtures/minimal_udp1001_session/capture.pcapng
!tests/fixtures/minimal_udp1001_session/manifest.jsonl
tests/fixtures/minimal_udp1001_session/udp_ipbox_export.txt
*.egg-info/
.pytest_cache/

# Editor / IDE
.vscode/

# HA add-on build staging (populated by prepare-build.sh / CI)
ipbuilding_gateway/gateway/
